# セキュリティモニター テスト仕様書

作成日: 2026-05-13  
対象システム: security_monitor_2（フェーズ4完成版）  
テスト実施者: tsystem 
テスト実施日: 2026-05-14 

---

## 前提条件

- rpi4-1（192.168.0.60）、rpi4-0（192.168.0.58）、vostro（192.168.0.154）が起動済みであること
- AtomCam 3台（192.168.0.12 / 192.168.0.7 / 192.168.0.16）が起動済みであること
- テスト端末（ブラウザ確認用）がLAN内に接続済みであること

---

## テスト結果凡例

| 記号 | 意味 |
|---|---|
| OK | 期待結果と一致 |
| NG | 期待結果と不一致 |
| N/A | 環境上、実施対象外 |

---

## ID 表記

| プレフィックス | 略 | 意味 |
|---|---|---|
| TG-NN | Test Group | テストグループ（テスト項目の分類単位） |
| TNN | Test | テストグループ内の個別テスト項目 |
| TC-APP-NNN | Test Case（APP=app.js） | app.js の Playwright テストケース |

---

## TG-01: 事前確認（サービス起動・疎通確認）

### T01: rpi4-1 サービス起動確認

**前提**: rpi4-1 に SSH でログイン済み

| コマンド | 確認内容 |
|---|---|
| `systemctl is-active mediamtx` | `active` が返ること |
| `systemctl is-active coturn` | `active` が返ること |
| `systemctl is-active nginx` | `active` が返ること |
| `systemctl is-active push-daemon` | `active` が返ること |
| `systemctl is-active collect-metrics.timer` | `active` が返ること |

**テスト結果**: OK 
**備考**: ____________________________________________

---

### T02: ネットワーク疎通確認

**前提**: テスト端末からコマンド実行

| 対象 | コマンド | 期待結果 |
|---|---|---|
| rpi4-1 HTTP | `curl -s -o /dev/null -w "%{http_code}" http://192.168.0.60/` | `200` |
| AtomCam Swing #1 | `ping -c 3 192.168.0.12` | パケットロスなし |
| AtomCam 2 | `ping -c 3 192.168.0.7` | パケットロスなし |
| AtomCam Swing #2 | `ping -c 3 192.168.0.16` | パケットロスなし |
| rpi4-0 | `ping -c 3 192.168.0.58` | パケットロスなし |
| vostro | `ping -c 3 192.168.0.154` | パケットロスなし |

**テスト結果**: OK
**備考**: ____________________________________________

---

## TG-02: カメラ映像表示テスト

**前提**: ブラウザで `http://192.168.0.60/` を開く

### T03: 3カメラ同時WebRTC表示

**手順**:
1. ブラウザで `http://192.168.0.60/` を開く
2. 画面に 2×2 グリッドが表示されるまで待機（最大30秒）

**期待結果**: 上段2枠・下段左枠に映像が表示され、下段右枠にメトリクスグラフが表示されます  
**テスト結果**: OK
**備考**: ____________________________________________

---

### T04: 映像遅延目視確認

**手順**:
1. カメラ前で手を振り、映像の遅延時間を目視で確認する

**期待結果**: 実際の動作から概ね 2 秒以内に映像に反映されます  
**テスト結果**: OK
**備考**: ____________________________________________

---

### T05: 全画面表示切り替え

**手順**:
1. いずれかのカメラ映像枠をクリックする
2. 当該映像が全画面（ウィンドウ全体）に拡大されることを確認する
3. 再度クリック（または Esc キー）で元の 2×2 グリッドに戻ることを確認する

**期待結果**:
- クリック後: 選択カメラが全画面表示、他の映像・グラフは非表示
- 再クリック後: 2×2 グリッドに復帰

**テスト結果**: OK
**備考**: ____________________________________________

---

### T06: 映像 stall 自動回復

**手順**:

1. rpi4-1 で対象カメラの MediaMTX パスを一時停止する（または AtomCam の電源を一時切断する）
2. ブラウザ上で映像が止まること（フリーズ）を確認する
3. 40秒程度待機し、映像が自動で再接続・再表示されることを確認する
4. カメラを復旧させ、映像が正常に戻ることを確認する

**期待結果**: フリーズ検出後、約 30 秒以内に自動再接続し映像が復帰します  
**テスト結果**: OK
**備考**: ____________________________________________

---

## TG-03: メトリクス表示テスト

**前提**: ブラウザで `http://192.168.0.60/` を開き、右下パネルを確認する

### T07: rpi4-1 メトリクスグラフ表示

**確認内容**:
- `rpi4-1` の CPU 使用率グラフが表示されること
- `rpi4-1` の CPU 温度グラフが表示されること
- `rpi4-1` のディスク I/O グラフが表示されること

**テスト結果**: OK  
**備考**: ____________________________________________

---

### T08: Linux クライアント（vostro）メトリクスグラフ表示

**確認内容**:
- `vostro` の CPU 使用率グラフが表示されること
- `vostro` の CPU 温度グラフが表示されること

**テスト結果**: OK
**備考**: ____________________________________________

---

### T09: Windows クライアント メトリクスグラフ表示

**手順**:
1. Windows クライアントが LAN に接続されており、タスクスケジューラが有効であることを確認する
2. `http://192.168.0.60/` のグラフパネルで Windows クライアントのメトリクスが表示されることを確認する

**確認内容**:
- Windows クライアントの CPU 使用率グラフが表示されること
- Windows クライアントのメモリ使用率グラフが表示されること

**テスト結果**: OK
**備考**: ____________________________________________

---

### T10: グラフ更新間隔確認

**手順**:
1. ブラウザのコンソール（F12）で XHR リクエストを監視する
2. 30 秒間隔でメトリクスファイルの取得リクエストが発行されることを確認する

**期待結果**: 30 秒間隔でグラフが更新されます  
**テスト結果**: OK
**備考**: ____________________________________________

---

## TG-04: 録画・バックアップテスト

### T11: AtomCam 録画 → Samba 保存確認

**手順**:

1. rpi4-0 に SSH でログインする
2. 以下コマンドで録画ファイルが保存されていることを確認する

```bash
ls -lhrt /mnt/usb_share/ | tail -20
```

**期待結果**: AtomCam 3台分の日付別ディレクトリに 1分単位の MP4 ファイルが存在します  
**テスト結果**: OK
**備考**: ____________________________________________

---

### T12: macmini2010 へのバックアップ確認

**前提**: macmini2010 が起動している場合のみ実施（不定期起動のため N/A 可）

**手順**:
1. macmini2010 に SSH でログインする
2. 以下コマンドでバックアップファイルが存在することを確認する

```bash
ls -lhrt /mnt/usb_disk/ | tail -20
```

**期待結果**: rpi4-0 の録画ファイルが rsync により転送されたファイルが存在します  
**テスト結果**: OK
**備考**: ____________________________________________

---

## TG-05: キオスクモードテスト

### T13: vostro 自動起動確認

**手順**:
1. vostro を再起動する（`sudo reboot`）
2. デスクトップ環境起動後、Google Chrome が自動で `http://192.168.0.60/` を全画面で開くことを確認する

**期待結果**: ログイン後、手動操作なしでセキュリティモニター画面が全画面表示されます  
**テスト結果**: OK
**備考**: ____________________________________________

---

### T14: スクリーンセーバー・画面オフ無効化確認

**手順**:
1. vostro でセキュリティモニター画面表示中に 30 分以上放置する

**期待結果**: 画面が消灯せず、スクリーンセーバーが起動しないこと  
**テスト結果**: OK
**備考**: ____________________________________________

---

## TG-06: 安定稼働テスト

### T15: 24時間稼働後 映像表示継続確認

**手順**:
1. システムを起動した状態で 24 時間放置する
2. 24 時間後にブラウザで `http://192.168.0.60/` を確認する

**期待結果**: 3 カメラすべての映像が正常に表示され続けていること  
**稼働開始日時**: 2026-05-14　00:13:00
**確認日時**: 2026-05-15　14:10:00  
**テスト結果**: OK
**備考**: ____________________________________________

---

### T16: 24時間稼働後 メトリクスグラフ継続確認

**手順**:
1. T15 と同タイミングで確認する

**期待結果**: 各ノードのメトリクスグラフが直近 30 秒以内のデータを表示していること  
**テスト結果**: OK
**備考**: ____________________________________________

---

## TG-07: backup.sh テスト

**前提**: テスト端末（macmini 等）で実施。SSH 接続可能なリモートホストが必要なテストは indo@rpi4-1 を使用する。

---

### TB01: user@host 未指定でエラー終了

**手順**:
```bash
cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../backup.sh
```

**期待結果**: `ERROR: user@host が指定されていません` が表示され、`exit 1` で終了します  
**テスト結果**: OK  
**備考**: 2026-05-19

---

### TB02: backup.txt が存在しないケース

**手順**:
```bash
cd /tmp
/home/indo/Public/security_monitor_2/inventory/backup.sh dummy@host
```

**期待結果**: `ERROR: ./backup.txt が見つかりません` が表示され、`exit 1` で終了します  
**テスト結果**: OK
**備考**: 2026-05-19

---

### TB03: -c で別定義ファイル指定

**前提**: `/tmp/tb03.txt` に有効な1行を記載しておく

**手順**:

```bash
# 定義ファイル準備（リモートに確実に存在するファイルを指定）
echo "@var@www@html@index.html /var/www/html/index.html" > /tmp/tb03.txt

cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../backup.sh -c /tmp/tb03.txt indo@rpi4-1
```

**期待結果**: `/tmp/tb03.txt` を読み込んで処理が実行されます（`START` 〜 `完了:` ログが出力されます）  
**テスト結果**: OK
**備考**: 2026-05-19

---

### TB04: リモートファイルが取得不可（scp失敗）

**前提**: `/tmp/tb04.txt` に存在しないリモートパスを指定した行を記載する

**手順**:
```bash
echo "dummy_local /tmp/nonexistent_file_tb04" > /tmp/tb04.txt
cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../backup.sh -c /tmp/tb04.txt indo@rpi4-1
```

**期待結果**: `ERROR: リモートファイルの取得失敗` が表示され、エラー件数が 1 加算された上で処理が継続します  
**テスト結果**: OK
**備考**: 2026-05-19

---

### TB05: ローカルファイルが存在しない（新規バックアップ・確認）

**前提**: リモートに存在し、ローカルに存在しないファイルを定義ファイルに指定する

**手順**:
```bash
# ローカルファイルを削除しておく
TMPDIR_L="/tmp/tb05_local" && mkdir -p "$TMPDIR_L"
echo "${TMPDIR_L}/index.html /var/www/html/index.html" > /tmp/tb05.txt

cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../backup.sh -c /tmp/tb05.txt indo@rpi4-1
ls -la /tmp/tb05_local/
```

**期待結果**:
- `ローカルにファイルが存在しません。新規バックアップしますか?` のプロンプトが表示されます
- `Y` 入力: `バックアップしました。(新規): indo@rpi4-1:/var/www/html/index.html → ...` が表示され、ローカルにファイルが生成されます
- `S` 入力: `バックアップをスキップしました。:` が表示されます
- `C` 入力: `CANCEL:` が表示されて exit 0 で終了します

**テスト結果**: OK
**備考**: 2026-05-19

---

### TB06: local と remote に差分なし（スキップ）

**前提**: ローカルとリモートが同一内容・同一タイムスタンプのファイルを用意する

**手順**:
```bash
# rpi4-1 からファイルを取得してタイムスタンプを合わせる
scp -p indo@rpi4-1:/var/www/html/index.html /tmp/tb06_index.html
echo "/tmp/tb06_index.html /var/www/html/index.html" > /tmp/tb06.txt

cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../backup.sh -c /tmp/tb06.txt indo@rpi4-1
```

**期待結果**: `差分なし (...)` および `バックアップをスキップしました。` が表示されます  
**テスト結果**: OK
**備考**: 2026-05-19

---

### TB07: 差分あり・remote が新しい（確認プロンプト）

**前提**: リモートの本番ファイルを基にローカル側へ内容差分を作り、古い日付に設定する

**手順**:
```bash
scp -p indo@rpi4-1:/var/www/html/index.html /tmp/tb07_index.html
echo "<!-- tb07 local edit -->" >> /tmp/tb07_index.html   # ローカル内容に差分を作る
touch -t 200001010000 /tmp/tb07_index.html               # ローカルを古い日付に
echo "/tmp/tb07_index.html /var/www/html/index.html" > /tmp/tb07.txt

cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../backup.sh -c /tmp/tb07.txt indo@rpi4-1
```

**期待結果**:
- `ファイルに差分があります。バックアップしますか?` のプロンプトが表示され、補足に `※ remote が新しい` が出ます
- `Y` 入力: `バックアップしました。: indo@rpi4-1:... → /tmp/tb07_index.html` が表示され、ローカルがリモート内容に更新されます
- `S` 入力: `バックアップをスキップしました。:` が表示されます
- `C` 入力: `CANCEL:` が表示されて exit 0 で終了します

**テスト結果**: OK
**備考**: 2026-05-19

---

### TB08: 差分あり・local が新しい（確認プロンプト）

**前提**: リモートの本番ファイルを基にローカル側へ内容差分を作り、現在日時に設定する

**手順**:
```bash
scp -p indo@rpi4-1:/var/www/html/index.html /tmp/tb08_index.html
echo "<!-- tb08 local edit -->" >> /tmp/tb08_index.html   # ローカル内容に差分を作る
touch /tmp/tb08_index.html                                # ローカルを現在日時に
echo "/tmp/tb08_index.html /var/www/html/index.html" > /tmp/tb08.txt

cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../backup.sh -c /tmp/tb08.txt indo@rpi4-1
```

**期待結果**:
- `ファイルに差分があります。バックアップしますか?` のプロンプトが表示され、補足に `※ local が新しい` が出ます
- `Y` 入力: `バックアップしました。: indo@rpi4-1:... → /tmp/tb08_index.html` が表示され、ローカルがリモート内容で上書きされます
- `S` 入力: `バックアップをスキップしました。:` が表示されます
- `C` 入力: `CANCEL:` が表示されて exit 0 で終了します

**テスト結果**: OK
**備考**: 2026-05-19

---

### TB09: エラーあり終了時 exit_code=1

**手順**: TB04 と同様の手順で実行後、終了コードを確認する

```bash
echo $?
```

**期待結果**: `1` が返ります（ログ末尾に `END rc: 1` が記録されます）  
**テスト結果**: OK
**備考**: 2026-05-19

---

## TG-08: release.sh テスト

**前提**: テスト端末（macmini 等）で実施。SSH 接続可能なリモートホストが必要なテストは indo@rpi4-1 を使用する。

---

### TR01: user@host 未指定でエラー終了

**手順**:
```bash
cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../release.sh
```

**期待結果**: `ERROR: user@host が指定されていません` が表示され、`exit 1` で終了します  
**テスト結果**: OK
**備考**: 2026-05-19

---

### TR02: release.txt が存在しないケース

**手順**:
```bash
cd /tmp
/home/indo/Public/security_monitor_2/inventory/release.sh dummy@host
```

**期待結果**: `ERROR: ./release.txt が見つかりません` が表示され、`exit 1` で終了します  
**テスト結果**: OK
**備考**: 2026-05-19

---

### TR03: -c で別定義ファイル指定

**前提**: `/tmp/tr03.txt` に有効な1行（ホームディレクトリ以下のパス）を記載しておく

**手順**:
```bash
echo "/tmp/tr03_dummy.txt /home/indo/tr03_dummy.txt" > /tmp/tr03.txt
echo "test" > /tmp/tr03_dummy.txt

cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../release.sh -c /tmp/tr03.txt indo@rpi4-1
```

**期待結果**: `/tmp/tr03.txt` を読み込んで処理が実行されます  
**テスト結果**: OK  
**備考**: 2026-05-19

---

### TR04: ローカルファイルが存在しない（エラーカウント・継続）

**手順**:
```bash
echo "/tmp/tr04_nonexistent.txt /home/indo/tr04.txt" > /tmp/tr04.txt

cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../release.sh -c /tmp/tr04.txt indo@rpi4-1
```

**期待結果**: `ERROR: ローカルファイルが存在しません` が表示され、エラー件数が 1 加算された上で処理が継続します  
**テスト結果**: OK
**備考**: 2026-05-19

---

### TR05: リモートファイルが存在しない（新規リリース・確認）

**手順**:
```bash
echo "test_new" > /tmp/tr05_new.txt
# リモートに存在しないパスを指定
echo "/tmp/tr05_new.txt /home/indo/tr05_new_$(date +%s).txt" > /tmp/tr05.txt

cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../release.sh -c /tmp/tr05.txt indo@rpi4-1
```

**期待結果**:
- `リモートにファイルが存在しません。新規リリースしますか?` のプロンプトが表示されます
- `Y` 入力: `リリースしました。(新規):` が表示され、リモートにファイルが作成されます
- `S` 入力: `リリースをスキップしました。:` が表示されます
- `C` 入力: `CANCEL:` が表示されて exit 0 で終了します

**テスト結果**: OK
**備考**: 2026-05-19

---

### TR06: local と remote に差分なし（スキップ）

**前提**: リモートにすでに同一内容・同一タイムスタンプのファイルが存在する

**手順**:
```bash
# リモートのファイルをローカルに取得してタイムスタンプを合わせる
scp -p indo@rpi4-1:/var/www/html/index.html /tmp/tr06_index.html
echo "/tmp/tr06_index.html /var/www/html/index.html" > /tmp/tr06.txt

cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../release.sh -c /tmp/tr06.txt indo@rpi4-1
```

**期待結果**: `差分なし (...)` および `リリースをスキップしました。` が表示されます  
**テスト結果**: OK
**備考**: 2026-05-19

---

### TR07: 差分あり・local が新しい（確認プロンプト）

**前提**: リモートにテスト用ファイルを配置し、ローカル側に内容差分を作る

**手順**:
```bash
# リリース先は本番ファイルを避け throwaway パスを使う
echo "tr07 original" > /tmp/tr07_test.txt
scp -p /tmp/tr07_test.txt indo@rpi4-1:/home/indo/tr07_test.txt
echo "tr07 modified" > /tmp/tr07_test.txt          # ローカル内容を変更（mtime も現在日時に更新）
echo "/tmp/tr07_test.txt /home/indo/tr07_test.txt" > /tmp/tr07.txt

cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../release.sh -c /tmp/tr07.txt indo@rpi4-1
```

**期待結果**:
- `ファイルに差分があります。リリースしますか?` のプロンプトが表示され、補足に `※ local が新しい` が出ます
- プロンプト末尾は `[S)kip / Y)es / D)iff / C)ancel]:` となります
- `Y` 入力: `リリースしました。:` が表示されリモートへ転送されます
- `S` 入力: `リリースをスキップしました。:` が表示されます
- `D` 入力: `diff -u remote local` が `${PAGER:-less}` 経由で表示され、終了後に再度同じプロンプトが表示されます
- `C` 入力: `CANCEL:` が表示されて exit 0 で終了します

**後始末**: `ssh indo@rpi4-1 "rm -f /home/indo/tr07_test.txt"`

**テスト結果**: OK
**備考**: 2026-05-19

---

### TR08: 差分あり・remote が新しい（確認プロンプト）

**前提**: リモートにテスト用ファイルを配置し、ローカル側を古い日付・内容差分にする

**手順**:
```bash
echo "tr08 original" > /tmp/tr08_test.txt
scp -p /tmp/tr08_test.txt indo@rpi4-1:/home/indo/tr08_test.txt
echo "tr08 modified" > /tmp/tr08_test.txt          # ローカル内容を変更
touch -t 200001010000 /tmp/tr08_test.txt           # ローカルを古い日付に
echo "/tmp/tr08_test.txt /home/indo/tr08_test.txt" > /tmp/tr08.txt

cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../release.sh -c /tmp/tr08.txt indo@rpi4-1
```

**期待結果**:
- `ファイルに差分があります。リリースしますか?` のプロンプトが表示され、補足に `※ remote が新しい` が出ます
- プロンプト末尾は `[S)kip / Y)es / D)iff / C)ancel]:` となります
- `Y` 入力: `リリースしました。:` が表示されリモートへ転送されます
- `S` 入力: `リリースをスキップしました。:` が表示されます
- `D` 入力: `diff -u remote local` が `${PAGER:-less}` 経由で表示され、終了後に再度同じプロンプトが表示されます
- `C` 入力: `CANCEL:` が表示されて exit 0 で終了します

**後始末**: `ssh indo@rpi4-1 "rm -f /home/indo/tr08_test.txt"`

**テスト結果**: OK  
**備考**: 2026-05-19

---

### TR09: /etc/ 以下のパスは /tmp 経由 sudo mv で配置

**前提**: rpi4-1 で `sudo mv` が実行できること（sudoers 設定済み）

**手順**:
```bash
# テスト用に /etc/ 以下への書き込みが sudo で可能なパスを使う
echo "test_etc" > /tmp/tr09_test.conf
echo "/tmp/tr09_test.conf /etc/tr09_test.conf" > /tmp/tr09.txt

cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../release.sh -c /tmp/tr09.txt indo@rpi4-1

# リモートで確認
ssh indo@rpi4-1 "ls -la /etc/tr09_test.conf && cat /etc/tr09_test.conf"

# 後始末
ssh indo@rpi4-1 "sudo rm /etc/tr09_test.conf"
```

**期待結果**: `新規リリースしますか?` のプロンプトで `Y` を入力すると `/etc/tr09_test.conf` がリモートに作成され、内容が `test_etc` と一致します  
**テスト結果**: OK
**備考**: 2026-05-19

---

### TR10: エラーあり終了時 exit_code=1

**手順**: TR04 と同様の手順で実行後、終了コードを確認する

```bash
echo $?
```

**期待結果**: `1` が返ります（ログ末尾に `END rc: 1` が記録されます）  
**テスト結果**: OK
**備考**: 2026-05-19

---

### TR11: 差分あり・D 入力で diff 表示後に再プロンプト

**前提**: リモートにテスト用ファイルを配置し、ローカル側に内容差分を作る

**手順**:
```bash
echo "tr11 original" > /tmp/tr11_test.txt
scp -p /tmp/tr11_test.txt indo@rpi4-1:/home/indo/tr11_test.txt
echo "tr11 modified" > /tmp/tr11_test.txt          # ローカル内容を変更
echo "/tmp/tr11_test.txt /home/indo/tr11_test.txt" > /tmp/tr11.txt

cd /home/indo/Public/security_monitor_2/inventory/rpi4-1
../release.sh -c /tmp/tr11.txt indo@rpi4-1
```

**期待結果**:
- プロンプト末尾は `[S)kip / Y)es / D)iff / C)ancel]:` となります
- `D` 入力: `diff -u remote local` が `${PAGER:-less}` 経由で表示されます
  - `+tr11 modified`（local 側、追加）と `-tr11 original`（remote 側、削除）が含まれます
  - less から `q` で抜けると、同じプロンプトが再表示されます
- 再表示後に `D` を再入力すると、再び diff が表示されます（繰り返し可能）
- 再表示後に `Y` を入力すると、リリースされます
- 再表示後に `S` を入力すると、スキップされます
- 再表示後に `C` を入力すると、`CANCEL:` が表示されて exit 0 で終了します

**後始末**: `ssh indo@rpi4-1 "rm -f /home/indo/tr11_test.txt"`

**テスト結果**:
**備考**:

---

---

## TG-09: app.js Playwright テスト

**前提**: `test/playwright/` で `npm install` が完了済みであること（システムインストールの Google Chrome を使用するため追加のブラウザダウンロード不要）。  
インターネット接続があること（A〜C グループは Chart.js を CDN から取得するため）。

```bash
cd test/playwright
npx playwright test --reporter=list
```

### A: 基本動作（TC-APP-010〜080）

| ID | 確認内容 | テスト結果 | 備考 |
|---|---|---|---|
| TC-APP-010 | `#clock` に `YYYY-MM-DD HH:MM:SS` 形式の時刻が表示される | OK | 2026-05-19 |
| TC-APP-020 | `#metrics-status` が「正常」になる（NDJSON 取得成功、`#fso-status` も更新される） | OK | 2026-05-19 |
| TC-APP-030 | `chart-cpu/temp/disk/mem` の `<canvas>` 要素が存在する | OK | 2026-05-19 |
| TC-APP-040 | `#panel-metrics` クリックで全画面オーバーレイが開く | OK | 2026-05-19 |
| TC-APP-050 | オーバーレイ余白クリックで全画面オーバーレイが閉じる | OK | 2026-05-19 |
| TC-APP-060 | `#fso-back-btn` クリックで全画面オーバーレイが閉じる | OK | 2026-05-19 |
| TC-APP-070 | 未接続時にカメラパネルをクリックしても `#fullscreen-overlay` が開かない | OK | 2026-05-19 |
| TC-APP-080 | WHEP POST 404 後にカメラバッジが「切断」になる | OK | 2026-05-19 |

### B: ネットワークエラー（TC-APP-090〜120）

`page.route()` で各経路を制御してエラーパスを通過させる。

| ID | 確認内容 | テスト結果 | 備考 |
|---|---|---|---|
| TC-APP-090 | `/api/my-history` 404 → `fetchNdjson` が `[]` を返す → `#metrics-status` が「正常」 | OK | 2026-05-19 |
| TC-APP-100 | `/api/my-history` 500 → `fetchNdjson` が throw → `refreshMetrics` catch → `#metrics-status` と `#fso-status` が「取得失敗」 | OK | 2026-05-19 |
| TC-APP-110 | CDN abort → `script.onerror` 発火 → `#metrics-status` が「取得失敗」 | OK | 2026-05-19 |
| TC-APP-120 | 両 NDJSON 404 → `applyDataToChartSet` の `endSec<0` early return → クラッシュなし、チャートデータ空 | OK | 2026-05-19 |

### C: その他未通パス（TC-APP-130〜150）

`page.evaluate()` で内部関数を直接呼び出してカバーするパス。

| ID | 確認内容 | テスト結果 | 備考 |
|---|---|---|---|
| TC-APP-130 | `setStatus` に未知 state を渡す → `labels` fallback で raw string が表示される | OK | 2026-05-19 |
| TC-APP-140 | `openMetricsFullscreen` 2回目呼び出し → `fsoInitialized=true` パスを通る → FSO チャートが存在する | OK | 2026-05-19 |
| TC-APP-150 | overlay 開放中に `refreshMetrics` を 500 エラーで呼び出す → `#fso-status` が「取得失敗」に更新される | OK | 2026-05-19 |

### D: WebRTC モック（TC-APP-160〜230）

`page.addInitScript()` で `RTCPeerConnection` をモック差替え。`page.goto` は `domcontentloaded` を使用（Chart.js CDN を待たない）。

| ID | 確認内容 | テスト結果 | 備考 |
|---|---|---|---|
| TC-APP-160 | `startWhep` 成功 → `#status-swing1` が「接続済み」（ICE イベント含む） | OK | 2026-05-19 |
| TC-APP-170 | `startStallDetector` 動作 → 3秒間 currentTime 固定 → `#status-swing1` が「遅延」 | OK | 2026-05-19 |
| TC-APP-180 | stall 解消 → currentTime 増分 → `#status-swing1` が「接続済み」に復帰 | OK | 2026-05-19 |
| TC-APP-190 | `startStallDetector` 多重起動防止（`clearInterval` ブランチ）→ 再起動後も stall 検出が正常動作 | OK | 2026-05-19 |
| TC-APP-200 | `openFullscreen` 成功（srcObject あり）→ `#fullscreen-overlay` が表示される | OK | 2026-05-19 |
| TC-APP-210 | `closeFullscreen` via overlay クリック → `#fullscreen-overlay` が非表示になる | OK | 2026-05-19 |
| TC-APP-220 | `closeFullscreen` via 戻るボタン → `#fullscreen-overlay` が非表示になる | OK | 2026-05-19 |
| TC-APP-230 | `onconnectionstatechange(disconnected)` → `pc.close()` が呼ばれ `#status-swing1` が「切断」 | OK | 2026-05-19 |

---

## テスト総括

| テストグループ | 項目数 | OK | NG | N/A |
|---|---|---|---|---|
| TG-01: 事前確認 | 2 | 2 | | |
| TG-02: カメラ映像表示 | 4 | 4 | | |
| TG-03: メトリクス表示 | 4 | 4 | | |
| TG-04: 録画・バックアップ | 2 | 2 | | |
| TG-05: キオスクモード | 2 | 2 | | |
| TG-06: 安定稼働 | 2 | 2 | | |
| TG-07: backup.sh | 9 | 9 | | |
| TG-08: release.sh | 11 | 10 | | |
| TG-09: app.js Playwright | 23 | 23 | | |
| **合計** | **59** | 58 | | |

**総合判定**: OK
**総括コメント**:  

```
（自由記述）
```
